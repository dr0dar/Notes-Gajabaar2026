- A development environment is a set of tools for developing software.
- Termianl-based development workflows combine tools such as `tmux`(terminal multiplexer), `vim`(text editor), `bash`(shell) and a language-specific command-line tools such as `uv`, `Ruff` and `Mypy`.
---

You can learn vim through game. [Vim Adventures](https://vim-adventures.com/)

---
[[Some Fundamentals]]
